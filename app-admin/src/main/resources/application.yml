server:
  port: 8090
  servlet:
    context-path: /
spring:
  datasource:
    url: jdbc:mysql://rm-bp1gvs3ti016zt2hpvo.mysql.rds.aliyuncs.com:3306/ry-vue?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: 123456Asd
#  jackson:
#    date-format: yyyy-MM-dd HH:mm:ss # 全局时间格式
#    time-zone: GMT+8 # 指定默认时区，除非时间字段已指定时区，否则 JSON 序列化时都会使用此时区

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.hjy.**.domain
  configuration:
    map-underscore-to-camel-case: true

# 日志配置
logging:
  level:
    com.hjy: debug
